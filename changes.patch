diff --git a/.github/workflows/docker.yml b/.github/workflows/docker.yml
new file mode 100644
index 0000000..0430018
--- /dev/null
+++ b/.github/workflows/docker.yml
@@ -0,0 +1,31 @@
+name: Publish Docker image
+on:
+  push:
+    branches: [ main ]          # add other branches if you want, e.g. [ main, Content_Update ]
+  workflow_dispatch: {}         # allow manual runs from the Actions tab
+
+jobs:
+  build-and-push:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+
+      - name: Set tags
+        id: vars
+        run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
+
+      - uses: docker/setup-buildx-action@v3
+
+      - uses: docker/login-action@v3
+        with:
+          username: ${{ secrets.DOCKERHUB_USERNAME }}
+          password: ${{ secrets.DOCKERHUB_TOKEN }}
+
+      - uses: docker/build-push-action@v6
+        with:
+          context: .
+          platforms: linux/amd64,linux/arm64
+          push: true
+          tags: |
+            chickenchunk/portfolio-app:latest
+            chickenchunk/portfolio-app:${{ steps.vars.outputs.sha_short }}
\ No newline at end of file
diff --git a/.gitignore b/.gitignore
index cc7b141..79cca31 100644
--- a/.gitignore
+++ b/.gitignore
@@ -1,4 +1,6 @@
 # See https://docs.github.com/get-started/getting-started-with-git/ignoring-files for more about ignoring files.
+#environment variables
+.env.local
 
 # Compiled output
 /dist
